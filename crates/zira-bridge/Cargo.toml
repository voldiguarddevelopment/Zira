[package]
name = "zira-bridge"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
zira-config = { path = "../zira-config" }
zira-proto = { path = "../zira-proto" }

[lints]
workspace = true
